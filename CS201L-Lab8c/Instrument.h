#pragma once
#include <string>
#include <iomanip>
#include <iostream>
using namespace std;

class Instrument {
protected:
	string type;
	string name;
	string manufacturer;
	int year;
	int cost;
public:
    // Constructors
    // Getters (Accessors)
    // Setters (Mutators)
	// print
 
};